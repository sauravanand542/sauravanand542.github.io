---
import { profile } from "../data/profile";
---

<section class="section" id="contact" aria-labelledby="contact-heading">
  <div class="wrap">
    <div class="contact-panel" data-reveal>
      <p class="kicker">Contact</p>
      <h2 id="contact-heading">GitHub and LinkedIn</h2>
      <p>
        {profile.name} is based in {profile.location} and is open to full-time data engineering
        roles. Reach him on GitHub or LinkedIn.
      </p>
      <div class="contact-links">
        {
          profile.links.map((link) => (
            <a class="contact-link" href={link.href} rel="me">
              {link.label}
              <span class="sr-only"> — {link.href.replace(/^https?:\/\//, "")}</span>
            </a>
          ))
        }
      </div>
    </div>
  </div>
</section>
